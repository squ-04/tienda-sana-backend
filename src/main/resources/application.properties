#Mail
simplejavamail.smtp.host=smtp.gmail.com
simplejavamail.smtp.port=587
simplejavamail.smtp.username=tiendasana74@gmail.com
simplejavamail.smtp.password=jejb oomj ubpq ujvg
simplejavamail.smtp.transport.strategy=SMTP_TLS
spring.mail.properties.mail.smtp.auth=true
simplejavamail.debug=true

#Swagger
springdoc.use-management-port=true
management.server.port=8082
logging.level.org.springframework.boot.context.properties.bind=TRACE

# Exponer endpoints de actuator incluyendo prometheus
management.endpoints.web.exposure.include=*
management.endpoint.prometheus.enabled=true
management.endpoint.health.show-details=always
management.security.enabled=false

# Configurar m�tricas
management.metrics.tags.application=${spring.application.name}
management.prometheus.metrics.export.enabled=true

# Nombre de la aplicaci�n
spring.application.name=tienda-sana

# MongoDB (cat�logo admin: productos, proveedores, lotes, mesas administrativas)
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/tienda_sana}

# Cloudinary (firma server-side)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dpv25pud4}
cloudinary.api-key=${CLOUDINARY_API_KEY:996674287767736}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

# Para Tomcat (por defecto)
server.tomcat.threads.max=50
# si tus operaciones son lentas (p. ej. consultas pesadas a MongoDB).
server.tomcat.threads.min-spare=10
server.tomcat.accept-count=20
# Un valor m�s bajo hace que falle m�s r�pido bajo carga extrema,
# lo cual puede ser preferible a que muchas solicitudes esperen indefinidamente
# y consuman recursos del lado del cliente y del servidor.

#Reservas
reservas.pending-hold-minutes=15

# Mercado Pago (evita hardcode de ngrok)
mercadopago.access-token=${MERCADOPAGO_ACCESS_TOKEN:APP_USR-8178646482281064-100513-248819fc76ea7f7577f902e927eaefb7-2014458486}
mercadopago.frontend-base-url=${MERCADOPAGO_FRONTEND_BASE_URL:http://localhost:4200}
mercadopago.webhook-base-url=${MERCADOPAGO_WEBHOOK_BASE_URL:http://localhost:8080}